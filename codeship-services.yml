back:
  build:
    context: ./backend
    dockerfile: Dockerfile
  environment:
    - CI=true

web:
  build:
    context: ./frontend
    dockerfile: Dockerfile
  environment:
    - CI=true

test_runner:
  build:
    context: ./backend
    dockerfile: Dockerfile
  depends_on:
    - back
  environment:
    - CI=true